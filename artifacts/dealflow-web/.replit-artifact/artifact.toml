kind = "web"
previewPath = "/"
title = "DealFlow One"
version = "1.0.0"
id = "artifacts/dealflow-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25318

[services.development]
run = "pnpm --filter @workspace/dealflow-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/dealflow-web", "run", "build" ]
publicDir = "artifacts/dealflow-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25318"
BASE_PATH = "/"
